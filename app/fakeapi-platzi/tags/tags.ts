export enum TAG {
  product = "@products",
  create = "@create",
  smoke = "@smoke",
  negative = "@negative",
  positive = "@positive",
  annotations = "@annotations",
  schemaValidation = "@schemaValidation",
}
