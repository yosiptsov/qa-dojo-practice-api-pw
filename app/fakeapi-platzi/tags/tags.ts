export enum TAG {
  product = "@products",
  crud = "@crud",
  smoke = "@smoke",
  negative = "@negative",
  positive = "@positive",
  annotations = "@annotations",
  schemaValidation = "@schemaValidation",
  users = "@users",
}
