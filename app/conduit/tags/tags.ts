export enum TAG {
  auth = "@auth",
  nonAuthRequests = "@nonAuthRequests",
  isAuthOption = "@isAuthOption",
  authorized = "@authorized",
  user = "@user",
  article = "@article",
  crud = "@crud",
}
